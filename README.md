# Villivakkam Job Portal

pnpm/Turborepo monorepo · Hono + oRPC + valibot · Drizzle/Postgres+PostGIS ·
Meilisearch · Valkey/BullMQ · Node >=22, TypeScript 5.9, ESM.

## Setup

```sh
corepack enable
pnpm install
cp .env.example .env
docker compose up -d
pnpm dev
```

## Docs

See `docs/` (to come).
