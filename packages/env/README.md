# @jp/env

Typed, valibot-validated environment variables loaded once at module startup.
Throws a descriptive error (no secret values) on missing or invalid vars.
Copy `.env.example` to `.env` and fill in real values.
