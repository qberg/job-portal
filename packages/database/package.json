{
  "name": "@jp/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./init": "./src/init.ts",
    "./test/db-url": "./src/test/db-url.ts",
    "./migrate": "./src/migrate.ts",
    "./client": "./src/client.ts",
    "./types": "./src/types.ts",
    "./schema": "./src/schema/registry.ts",
    "./citizen-auth.schema": "./src/schema/kernel.schema/citizen-auth.schema.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "test": "vitest run --passWithNoTests",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  },
  "dependencies": {
    "drizzle-orm": "catalog:",
    "postgres": "catalog:",
    "@jp/env": "workspace:*",
    "@jp/logger": "workspace:*"
  },
  "devDependencies": {
    "drizzle-kit": "catalog:",
    "@jp/typescript-config": "workspace:*",
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vitest": "catalog:"
  }
}
